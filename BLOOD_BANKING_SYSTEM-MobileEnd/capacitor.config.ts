import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'BLOOD_BANKING_SYSTEM-MobileEnd',
  webDir: 'www'
};

export default config;
